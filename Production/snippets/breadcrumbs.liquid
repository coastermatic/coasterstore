<div class="breadcrumbs meta">

   
   {% if template contains "product" %}
      {% if collection %}
     <span><a href="/">Home</a></span> <span class="sep">/</span> <span><a href="{{ collection.url }}">{{ collection.title }}</a></span>
      {% endif %}
      {% if prduct.title == 'Gift Card' %}
     
      {% endif %}
      {% if prduct.title == 'Instagram Coasters' %}
     
      {% endif %}
    
   {% elsif template contains "list-collections" %}
      <span><a href="/">Home</a></span> <span class="sep">/</span> <span>{{ page_title }}</span>
   {% elsif template contains "collection" %}
      <span><a href="/">Home</a></span> <!--<span class="sep">/</span> <span><a href="/collections">Collections</a></span>--> <span class="sep">/</span> <span>{{ collection.title }}</span>
   
   {% elsif template contains "blog" %}
      <span><a href="/">Home</a></span> <span class="sep">/</span> <span>{{ blog.title }}</span>
      {% if current_tags.size > 0 %} 
      <span><a href="/">Home</a></span> <span class="sep">/</span> <span>Tagged: {% for tag in current_tags %}{{ tag }}{% unless forloop.last %}, {% endunless %}{% endfor %}</span>
      {% endif %}
   
   {% elsif template contains "page" %}
      
   
   {% elsif template contains "article" %}
      <span><a href="/">Home</a></span> <span class="sep">/</span> <span>{{ blog.title | link_to: blog.url }}</span>
      <span class="sep">/</span> <span>{{ article.title }}</span>
   {% elsif template contains "cart" %}
     
   {% elsif template contains "404" %}
      <span><a href="/">Home</a></span> <span class="sep">/</span> <span>404</span>
   {% elsif template contains "search" %}
     <span><a href="/">Home</a></span> <span class="sep">/</span> <span>Search results</span>
  
   {% elsif template contains "customers/login" %}
      <span class="sep">/</span> <span>{{ page_title }}</span>
   {% elsif template contains "customers/reset_password" %}
      <span class="sep">/</span> <span>{{ page_title }}</span>
   {% elsif template contains "customers/activate_account" %}
      <span class="sep">/</span> <span>{{ page_title }}</span>
   {% elsif template contains "customers/account" %}
      <span class="sep">/</span> <span>{{ page_title }}</span>
   {% elsif template contains "customers/addresses" %}
      <span class="sep">/</span> <span><a href="/account">Account</a></span> <span class="sep">/</span> <span>{{ page_title }}</span>
   {% elsif template contains "customers/register" %}
      <span class="sep">/</span> <span><a href="/account">Account</a></span> <span class="sep">/</span> <span>{{ page_title }}</span>
   {% elsif template contains "customers/order" %}
      <span class="sep">/</span> <span><a href="/account">Account</a></span> <span class="sep">/</span> <span>{{ page_title }}</span>
   {% endif %}

</div>